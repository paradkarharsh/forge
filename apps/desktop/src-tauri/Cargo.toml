[package]
name = "forge-desktop"
version = "0.1.0"
description = "Forge desktop workspace"
authors = ["Forge"]
edition = "2021"
rust-version = "1.77"
[lib]
name = "forge_desktop"
crate-type = ["staticlib", "cdylib", "rlib"]
[build-dependencies]
tauri-build = { version = "2", features = [] }
[dependencies]
tauri = { version = "2", features = [] }
